import React from "react";
import SparkSnapContainer from "./SparkSnapContainer";

const SparkSnapSearch = ({ searchTerm }) => {
  return (
    <div>
      <SparkSnapContainer searchTerm={searchTerm} />
    </div>
  );
};

export default SparkSnapSearch;